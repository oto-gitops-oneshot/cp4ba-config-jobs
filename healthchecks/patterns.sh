#!/bin/bash

# define deployment names for patterns in this file (space-separated)
content="-cpe-deploy -graphql-deploy"
document_processing="-mongo-deploy -gitgateway-deploy -viewone-deploy -cpds-deploy -cds-deploy"
css="-css-deploy-1"
tm="-tm-deploy"
ier="-ier-deploy"

